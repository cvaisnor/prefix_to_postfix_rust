#### Program to convert a prefix expression to an infix and postfix expression using a stack in Rust.

To compile and run with cargo:
```bash
cargo run
```

If you only want to run the compiled executable:
```bash
rustc main.rs
./main
```